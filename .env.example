# Copy to .env for local development (gitignored):
#   cp .env.example .env
#
# Used by docker-compose.yml. For ./mvnw spring-boot:run on the host, keep DB_HOST=localhost.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=ussd
DB_USERNAME=postgres
DB_PASSWORD=localpassword
APP_PORT=8080
